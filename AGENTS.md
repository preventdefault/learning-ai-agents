# Agents From Scratch

## Project Setup

1. Run `npm init --yes`
2. Set in package.json field `main` to `main.js`, and `type` to `module`
3. Add a NPM start script `node main.js`
4. Add dev dependency `@types/node`
5. Run `npm install`
6. Create an empty `main.js` file